diff --git a/src/app/rpg/combat/runtime_attacks.py b/src/app/rpg/combat/runtime_attacks.py
--- a/src/app/rpg/combat/runtime_attacks.py
+++ b/src/app/rpg/combat/runtime_attacks.py
@@ -95,6 +95,7 @@ def resolve_combat_attack(
         tick,
         resolution["defeated"],
     )
+    xp_result = safe_dict(loot_result.get("xp_result"))
 
     return {
         "resolved": True,
@@ -106,6 +107,7 @@ def resolve_combat_attack(
         "next_actor_id": next_actor_id,
         "ammo_result": deepcopy(safe_dict(ammo_result.get("result"))),
         "loot_result": deepcopy(loot_result),
+        "xp_result": deepcopy(xp_result),
         "combat_log_entry": deepcopy(combat_log_entry),
         "combat_state": deepcopy(combat_state),
         "tick": int(tick or 0),
diff --git a/src/tests/rpg/test_ci_deterministic_rpg_gates.py b/src/tests/rpg/test_ci_deterministic_rpg_gates.py
--- a/src/tests/rpg/test_ci_deterministic_rpg_gates.py
+++ b/src/tests/rpg/test_ci_deterministic_rpg_gates.py
@@ -119,5 +119,7 @@ def test_ci_combat_attack_result_exposes_nested_reward_xp():
     assert result["combat_ended"] is True
     assert xp_result["awarded"] is True
     assert xp_result["xp_awarded"] == 25
+    assert result["xp_result"]["awarded"] is True
+    assert result["xp_result"]["xp_awarded"] == 25
     assert state["player_state"]["level"] == 2
     assert state["player_state"]["xp"] == 15