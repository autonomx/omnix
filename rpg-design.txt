N73-N75.3 — Base Game Campaign Journal + Calendar Runtime v1
Goal
Move campaign time + player journal from test/report-only code into base RPG runtime.

Normal game sessions should maintain:
runtime_state.campaign_calendar
runtime_state.player_journal

Autoplay/report should read the same runtime data instead of inventing a separate test-only journal.

Strict rules:

Journal entries are deterministic summaries of already-authoritative turn facts.
Calendar/time is bounded runtime metadata.
Journal/calendar cannot mutate inventory, currency, quests, combat, travel, rewards, or NPC state.
LLM does not author journal entries in this bundle.
1. Add base runtime module

Create:

--- /dev/null
+++ src/app/rpg/campaign_journal.py
@@ -0,0 +1,282 @@
+from __future__ import annotations
+
+from copy import deepcopy
+from typing import Any, Dict, List
+
+
+CAMPAIGN_CALENDAR_VERSION = "campaign_calendar_v1"
+PLAYER_JOURNAL_VERSION = "player_journal_v1"
+
+SEASONS = ("spring", "summer", "autumn", "winter")
+
+
+def _safe_dict(value: Any) -> Dict[str, Any]:
+    return value if isinstance(value, dict) else {}
+
+
+def _safe_list(value: Any) -> List[Any]:
+    return value if isinstance(value, list) else []
+
+
+def _safe_str(value: Any) -> str:
+    return value if isinstance(value, str) else ""
+
+
+def campaign_time_for_turn(
+    *,
+    turn_index: int,
+    start_year: int = 1000,
+    start_day: int = 1,
+    minutes_per_turn: int = 30,
+) -> Dict[str, Any]:
+    """Deterministic lightweight fantasy calendar.
+
+    This is metadata only. It does not alter travel, rests, services, combat,
+    quests, or other authoritative simulation facts.
+    """
+    turn_index = max(1, int(turn_index or 1))
+    minutes_per_turn = max(1, int(minutes_per_turn or 30))
+    total_minutes = (turn_index - 1) * minutes_per_turn
+    day_offset = total_minutes // (24 * 60)
+    minute_of_day = total_minutes % (24 * 60)
+    hour = minute_of_day // 60
+    minute = minute_of_day % 60
+    day_of_year = ((start_day - 1 + day_offset) % 360) + 1
+    year = start_year + ((start_day - 1 + day_offset) // 360)
+    season = SEASONS[(day_of_year - 1) // 90]
+    day = ((day_of_year - 1) % 30) + 1
+    month = ((day_of_year - 1) // 30) + 1
+    if 5 <= hour < 12:
+        phase = "morning"
+    elif 12 <= hour < 17:
+        phase = "afternoon"
+    elif 17 <= hour < 21:
+        phase = "evening"
+    else:
+        phase = "night"
+    return {
+        "format_version": CAMPAIGN_CALENDAR_VERSION,
+        "year": year,
+        "season": season,
+        "month": month,
+        "day": day,
+        "day_of_year": day_of_year,
+        "hour": hour,
+        "minute": minute,
+        "time_label": f"{hour:02d}:{minute:02d}",
+        "day_phase": phase,
+        "absolute_day": int(day_offset) + 1,
+        "turn_index": turn_index,
+        "minutes_per_turn": minutes_per_turn,
+    }
+
+
+def campaign_journal_runtime_state(runtime_state: Dict[str, Any]) -> Dict[str, Any]:
+    runtime_state = runtime_state if isinstance(runtime_state, dict) else {}
+    runtime_state.setdefault(
+        "campaign_calendar",
+        {
+            "format_version": CAMPAIGN_CALENDAR_VERSION,
+            "minutes_per_turn": 30,
+            "current": campaign_time_for_turn(turn_index=1),
+            "history": [],
+        },
+    )
+    runtime_state.setdefault(
+        "player_journal",
+        {
+            "format_version": PLAYER_JOURNAL_VERSION,
+            "entries": [],
+            "pending_actions": [],
+            "pending_results": [],
+            "journal_every_turns": 4,
+            "max_entries": 100,
+        },
+    )
+    return runtime_state
+
+
+def _extract_player_action(
+    *,
+    player_input: str = "",
+    turn_contract: Dict[str, Any] | None = None,
+    turn_result: Dict[str, Any] | None = None,
+) -> str:
+    turn_contract = _safe_dict(turn_contract)
+    turn_result = _safe_dict(turn_result)
+    return (
+        _safe_str(player_input)
+        or _safe_str(turn_contract.get("player_input"))
+        or _safe_str(turn_contract.get("action"))
+        or _safe_str(turn_result.get("player_action"))
+        or _safe_str(turn_result.get("player_input"))
+    )
+
+
+def _extract_result_summary(
+    *,
+    turn_contract: Dict[str, Any] | None = None,
+    turn_result: Dict[str, Any] | None = None,
+) -> str:
+    turn_contract = _safe_dict(turn_contract)
+    turn_result = _safe_dict(turn_result)
+    resolved_result = _safe_dict(turn_contract.get("resolved_result"))
+    narration_payload = _safe_dict(turn_result.get("narration_payload"))
+    npc = _safe_dict(narration_payload.get("npc"))
+    parts = [
+        _safe_str(resolved_result.get("summary")),
+        _safe_str(resolved_result.get("description")),
+        _safe_str(turn_contract.get("result")),
+        _safe_str(narration_payload.get("action")),
+        _safe_str(narration_payload.get("narration")),
+        _safe_str(npc.get("line")),
+    ]
+    for part in parts:
+        if part:
+            return part[:700]
+    return ""
+
+
+def _journal_text(actions: List[str], results: List[str]) -> str:
+    action_part = "; ".join(actions[-4:]).strip()
+    result_part = " ".join(results[-2:]).strip()
+    if action_part and result_part:
+        return f"I focused on: {action_part}. What stood out: {result_part[:500]}"
+    if action_part:
+        return f"I focused on: {action_part}."
+    if result_part:
+        return f"What stood out: {result_part[:500]}"
+    return "I kept moving, watching for what changed around me."
+
+
+def advance_campaign_journal_for_turn(
+    *,
+    runtime_state: Dict[str, Any],
+    turn_index: int,
+    player_input: str = "",
+    turn_contract: Dict[str, Any] | None = None,
+    turn_result: Dict[str, Any] | None = None,
+    minutes_per_turn: int | None = None,
+    journal_every_turns: int | None = None,
+) -> Dict[str, Any]:
+    """Advance base-game calendar and deterministic player journal.
+
+    This mutates runtime_state only. It does not mutate authoritative
+    simulation facts.
+    """
+    runtime_state = campaign_journal_runtime_state(runtime_state)
+    calendar = _safe_dict(runtime_state.get("campaign_calendar"))
+    journal = _safe_dict(runtime_state.get("player_journal"))
+
+    if minutes_per_turn is None:
+        minutes_per_turn = int(calendar.get("minutes_per_turn") or 30)
+    if journal_every_turns is None:
+        journal_every_turns = int(journal.get("journal_every_turns") or 4)
+
+    time_info = campaign_time_for_turn(
+        turn_index=int(turn_index or 1),
+        minutes_per_turn=int(minutes_per_turn or 30),
+    )
+    calendar["minutes_per_turn"] = int(minutes_per_turn or 30)
+    calendar["current"] = time_info
+    history = _safe_list(calendar.get("history"))
+    if not history or _safe_dict(history[-1]).get("turn_index") != time_info["turn_index"]:
+        history.append(time_info)
+    calendar["history"] = history[-500:]
+
+    action = _extract_player_action(
+        player_input=player_input,
+        turn_contract=_safe_dict(turn_contract),
+        turn_result=_safe_dict(turn_result),
+    )
+    result = _extract_result_summary(
+        turn_contract=_safe_dict(turn_contract),
+        turn_result=_safe_dict(turn_result),
+    )
+
+    pending_actions = _safe_list(journal.get("pending_actions"))
+    pending_results = _safe_list(journal.get("pending_results"))
+    if action:
+        pending_actions.append(action[:300])
+    if result:
+        pending_results.append(result[:700])
+
+    journal["pending_actions"] = pending_actions[-12:]
+    journal["pending_results"] = pending_results[-12:]
+    journal["journal_every_turns"] = int(journal_every_turns or 4)
+
+    entries = _safe_list(journal.get("entries"))
+    should_write = int(turn_index or 1) % max(1, int(journal_every_turns or 4)) == 0
+    existing_entry_ids = {_safe_str(entry.get("entry_id")) for entry in entries if isinstance(entry, dict)}
+
+    if should_write:
+        entry_id = f"journal:turn:{int(turn_index or 1)}"
+        if entry_id not in existing_entry_ids:
+            entries.append(
+                {
+                    "format_version": PLAYER_JOURNAL_VERSION,
+                    "entry_id": entry_id,
+                    "start_turn": max(1, int(turn_index or 1) - len(pending_actions) + 1),
+                    "end_turn": int(turn_index or 1),
+                    "time": deepcopy(time_info),
+                    "perspective": "player",
+                    "text": _journal_text(pending_actions, pending_results),
+                    "source": "deterministic_runtime_journal",
+                }
+            )
+        journal["pending_actions"] = []
+        journal["pending_results"] = []
+
+    max_entries = int(journal.get("max_entries") or 100)
+    journal["entries"] = entries[-max_entries:]
+    runtime_state["campaign_calendar"] = calendar
+    runtime_state["player_journal"] = journal
+    return runtime_state
+
+
+def summarize_campaign_calendar(runtime_state: Dict[str, Any]) -> Dict[str, Any]:
+    runtime_state = _safe_dict(runtime_state)
+    calendar = _safe_dict(runtime_state.get("campaign_calendar"))
+    history = _safe_list(calendar.get("history"))
+    return {
+        "minutes_per_turn": calendar.get("minutes_per_turn", 30),
+        "turns_tracked": len(history),
+        "start": history[0] if history else _safe_dict(calendar.get("current")),
+        "end": _safe_dict(calendar.get("current")),
+        "rows": history[-20:],
+    }
+
+
+def summarize_player_journal(runtime_state: Dict[str, Any]) -> Dict[str, Any]:
+    runtime_state = _safe_dict(runtime_state)
+    journal = _safe_dict(runtime_state.get("player_journal"))
+    entries = _safe_list(journal.get("entries"))
+    return {
+        "entry_count": len(entries),
+        "entries": entries[-20:],
+        "pending_action_count": len(_safe_list(journal.get("pending_actions"))),
+        "pending_result_count": len(_safe_list(journal.get("pending_results"))),
+    }
2. Wire journal/calendar into base session turn path

You need to patch whichever base RPG session turn handler is authoritative in your app. Based on current structure, likely files are one of:

src/app/rpg/session.py
src/app/rpg/session_runtime.py
src/app/rpg/session_turn.py
src/app/rpg/runtime/session.py
src/app/api/rpg_session.py

Search:

Select-String -Path src\app\rpg\*.py,src\app\api\*.py -Pattern "turn_contract","session_id","runtime_state","/api/rpg/session/turn" -Context 3,8
2A. Add import to the turn handler file

In the file that builds/returns turn_result, add:

+from app.rpg.campaign_journal import advance_campaign_journal_for_turn
2B. Advance journal after the authoritative turn contract is built

Find the point after the turn contract/result exists and before the session is saved/returned. It will look roughly like one of these:

session["turns"].append(...)
session["turn_contract"] = turn_contract
save_session(session)
return {"ok": True, "session": session, "turn_contract": turn_contract}

Patch:

@@
+    runtime_state = session.setdefault("runtime_state", {})
+    turn_index = int(
+        turn_contract.get("turn_index")
+        or session.get("turn_index")
+        or len(session.get("turns", []))
+        or 1
+    )
+    session["runtime_state"] = advance_campaign_journal_for_turn(
+        runtime_state=runtime_state,
+        turn_index=turn_index,
+        player_input=player_input,
+        turn_contract=turn_contract,
+        turn_result={
+            "narration_payload": narration_payload if "narration_payload" in locals() else {},
+            "player_action": player_input,
+        },
+    )

If the local variable is named differently:

player_input may be action_text / input_text / command
turn_contract may be contract
narration_payload may be structured_narration / narration_result
session may be state / session_state

Use the equivalent values.

The important invariant:

advance_campaign_journal_for_turn runs after deterministic turn outcome exists,
but before session is persisted/returned.
2C. Include journal/calendar in session get response

If /api/rpg/session/get already returns full session, nothing else is needed.

If it filters runtime state, add:

@@
 response["campaign_calendar"] = session.get("runtime_state", {}).get("campaign_calendar", {})
 response["player_journal"] = session.get("runtime_state", {}).get("player_journal", {})

or expose under:

{
  "session": {
    "runtime_state": {
      "campaign_calendar": {},
      "player_journal": {}
    }
  }
}
3. Update autoplay/report to read base runtime journal first

Currently report_sections.py generates a test-only calendar/journal from transcript. Keep that as fallback, but prefer runtime state.

Patch:

src/tests/rpg/autoplay/report_sections.py
3A. Import base summarizers
--- src/tests/rpg/autoplay/report_sections.py
+++ src/tests/rpg/autoplay/report_sections.py
@@
 from typing import Any, Dict, List
+
+from app.rpg.campaign_journal import (
+    campaign_time_for_turn,
+    summarize_campaign_calendar,
+    summarize_player_journal,
+)

Then remove or rename the duplicate local campaign_time_for_turn(...) in this file. If easier, replace the local function body with a wrapper:

-def campaign_time_for_turn(...):
-    ...
+from app.rpg.campaign_journal import campaign_time_for_turn
3B. Add helper to find final runtime journal state

Add near _last_nonempty_row_value(...):

@@
 def _last_nonempty_row_value(transcript: List[Dict[str, Any]], key: str) -> Any:
@@
     return {}
+
+
+def _latest_runtime_state_with_journal(transcript: List[Dict[str, Any]]) -> Dict[str, Any]:
+    for row in reversed(transcript if isinstance(transcript, list) else []):
+        row = _safe_dict(row)
+        runtime_state = _safe_dict(row.get("runtime_state"))
+        if runtime_state.get("campaign_calendar") or runtime_state.get("player_journal"):
+            return runtime_state
+        turn_result = _safe_dict(row.get("turn_result"))
+        session = _safe_dict(turn_result.get("session"))
+        runtime_state = _safe_dict(session.get("runtime_state"))
+        if runtime_state.get("campaign_calendar") or runtime_state.get("player_journal"):
+            return runtime_state
+    return {}
3C. Prefer base runtime journal in build_campaign_calendar_and_journal(...)

At the start of build_campaign_calendar_and_journal(...), add:

@@
 def build_campaign_calendar_and_journal(
     transcript: List[Dict[str, Any]],
     *,
     minutes_per_turn: int = 30,
     journal_every_turns: int = 4,
 ) -> Dict[str, Any]:
+    runtime_state = _latest_runtime_state_with_journal(transcript)
+    if runtime_state:
+        calendar = summarize_campaign_calendar(runtime_state)
+        journal = summarize_player_journal(runtime_state)
+        if calendar.get("turns_tracked") or journal.get("entry_count"):
+            calendar["source"] = "base_runtime"
+            journal["source"] = "base_runtime"
+            return {
+                "calendar": calendar,
+                "journal": journal,
+            }
+
     calendar_rows: List[Dict[str, Any]] = []

At the fallback return, add:

@@
         "calendar": {
+            "source": "report_fallback",
             "minutes_per_turn": minutes_per_turn,
@@
         "journal": {
+            "source": "report_fallback",
             "entry_count": len(journal_entries),
4. Add frontend UI journal panel

This makes journal visible in the base game, not only report.

Patch likely file:

src/static/rpg.js

or the RPG UI script that renders session state.

Search:

Select-String -Path src\static\*.js -Pattern "world events","journal","rpg-top-panels","session.runtime_state","render" -Context 3,8
4A. Add HTML container

In the RPG page/template, likely:

src/templates/rpg.html
src/static/index.html

Add near world events / journal / inspector panels:

+<section id="rpg-player-journal-panel" class="rpg-panel">
+  <h3>Player Journal</h3>
+  <div id="rpg-campaign-time" class="rpg-campaign-time"></div>
+  <div id="rpg-player-journal-list" class="rpg-player-journal-list"></div>
+</section>

If panels are dynamically created in JS, add the equivalent DOM creation.

4B. Add JS render function

Patch src/static/rpg.js:

+function renderPlayerJournal(session) {
+  const runtime = (session && session.runtime_state) || {};
+  const calendar = runtime.campaign_calendar || {};
+  const journal = runtime.player_journal || {};
+  const timeEl = document.getElementById("rpg-campaign-time");
+  const listEl = document.getElementById("rpg-player-journal-list");
+  if (!timeEl || !listEl) return;
+
+  const current = calendar.current || {};
+  const timeLabel = [
+    current.season ? String(current.season) : "",
+    current.year ? "Year " + String(current.year) : "",
+    current.month ? "Month " + String(current.month) : "",
+    current.day ? "Day " + String(current.day) : "",
+    current.time_label ? String(current.time_label) : "",
+    current.day_phase ? "(" + String(current.day_phase) + ")" : ""
+  ].filter(Boolean).join(" · ");
+  timeEl.textContent = timeLabel || "Campaign time has not started yet.";
+
+  const entries = Array.isArray(journal.entries) ? journal.entries.slice(-8).reverse() : [];
+  if (!entries.length) {
+    listEl.innerHTML = '<div class="rpg-muted">No journal entries yet.</div>';
+    return;
+  }
+  listEl.innerHTML = entries.map((entry) => {
+    const time = entry.time || {};
+    const heading = [
+      entry.entry_id || "Journal Entry",
+      time.time_label || "",
+      time.day_phase || ""
+    ].filter(Boolean).join(" · ");
+    return `
+      <article class="rpg-journal-entry">
+        <h4>${escapeHtml(heading)}</h4>
+        <p>${escapeHtml(entry.text || "")}</p>
+      </article>
+    `;
+  }).join("");
+}

If escapeHtml does not exist in rpg.js, add:

+function escapeHtml(value) {
+  return String(value == null ? "" : value)
+    .replaceAll("&", "&amp;")
+    .replaceAll("<", "&lt;")
+    .replaceAll(">", "&gt;")
+    .replaceAll('"', "&quot;")
+    .replaceAll("'", "&#039;");
+}
4C. Call it after session updates

Find where the UI renders session after /api/rpg/session/get or turn response. It may call things like:

renderWorldEvents(session)
renderInspector(session)
renderTopPanels(session)

Add:

+renderPlayerJournal(session);

For turn response shaped as { session: ... }:

+if (data && data.session) renderPlayerJournal(data.session);
4D. Add CSS

Patch your RPG CSS, likely:

src/static/rpg.css
src/static/styles.css

Add:

+.rpg-campaign-time {
+  font-size: 0.9rem;
+  opacity: 0.85;
+  margin-bottom: 0.5rem;
+}
+
+.rpg-player-journal-list {
+  display: flex;
+  flex-direction: column;
+  gap: 0.5rem;
+}
+
+.rpg-journal-entry {
+  border: 1px solid rgba(255,255,255,0.12);
+  border-radius: 10px;
+  padding: 0.65rem;
+  background: rgba(255,255,255,0.04);
+}
+
+.rpg-journal-entry h4 {
+  margin: 0 0 0.35rem 0;
+  font-size: 0.9rem;
+}
+
+.rpg-journal-entry p {
+  margin: 0;
+  line-height: 1.35;
+}

Adjust colors if your UI is light themed.

5. Add tests
5A. Base journal unit tests

Create:

--- /dev/null
+++ src/tests/rpg/test_campaign_journal.py
@@ -0,0 +1,95 @@
+from app.rpg.campaign_journal import (
+    advance_campaign_journal_for_turn,
+    campaign_time_for_turn,
+    summarize_campaign_calendar,
+    summarize_player_journal,
+)
+
+
+def test_campaign_time_for_turn_tracks_phase_and_date():
+    first = campaign_time_for_turn(turn_index=1, minutes_per_turn=60)
+    eighth = campaign_time_for_turn(turn_index=8, minutes_per_turn=60)
+
+    assert first["time_label"] == "00:00"
+    assert first["day_phase"] == "night"
+    assert eighth["time_label"] == "07:00"
+    assert eighth["day_phase"] == "morning"
+    assert eighth["season"] == "spring"
+
+
+def test_advance_campaign_journal_writes_entry_every_n_turns():
+    runtime_state = {}
+    for turn in range(1, 5):
+        runtime_state = advance_campaign_journal_for_turn(
+            runtime_state=runtime_state,
+            turn_index=turn,
+            player_input=f"I do thing {turn}",
+            turn_contract={
+                "turn_index": turn,
+                "player_input": f"I do thing {turn}",
+                "resolved_result": {"summary": f"Result {turn}"},
+            },
+            journal_every_turns=4,
+            minutes_per_turn=60,
+        )
+
+    journal = summarize_player_journal(runtime_state)
+    calendar = summarize_campaign_calendar(runtime_state)
+
+    assert calendar["turns_tracked"] == 4
+    assert journal["entry_count"] == 1
+    assert journal["entries"][0]["entry_id"] == "journal:turn:4"
+    assert "I do thing 1" in journal["entries"][0]["text"]
+    assert "Result" in journal["entries"][0]["text"]
+
+
+def test_advance_campaign_journal_is_idempotent_for_same_turn_entry():
+    runtime_state = {}
+    for _ in range(2):
+        runtime_state = advance_campaign_journal_for_turn(
+            runtime_state=runtime_state,
+            turn_index=4,
+            player_input="I ask Bran.",
+            turn_contract={
+                "turn_index": 4,
+                "player_input": "I ask Bran.",
+                "resolved_result": {"summary": "Bran answers."},
+            },
+            journal_every_turns=4,
+        )
+
+    journal = summarize_player_journal(runtime_state)
+    assert journal["entry_count"] == 1
5B. Report fallback test

Patch:

src/tests/rpg/autoplay/test_report_sections.py

Add:

@@
 def test_build_campaign_calendar_and_journal_creates_player_entries():
@@
     assert "I ask Bran" in result["journal"]["entries"][0]["text"]
+
+
+def test_build_campaign_calendar_and_journal_prefers_base_runtime_journal():
+    transcript = [
+        {
+            "turn_index": 4,
+            "runtime_state": {
+                "campaign_calendar": {
+                    "minutes_per_turn": 60,
+                    "current": {
+                        "turn_index": 4,
+                        "year": 1000,
+                        "season": "spring",
+                        "month": 1,
+                        "day": 1,
+                        "time_label": "03:00",
+                        "day_phase": "night",
+                    },
+                    "history": [
+                        {"turn_index": 1, "time_label": "00:00"},
+                        {"turn_index": 2, "time_label": "01:00"},
+                        {"turn_index": 3, "time_label": "02:00"},
+                        {"turn_index": 4, "time_label": "03:00"},
+                    ],
+                },
+                "player_journal": {
+                    "entries": [
+                        {
+                            "entry_id": "journal:turn:4",
+                            "text": "Base runtime journal entry.",
+                        }
+                    ]
+                },
+            },
+        }
+    ]
+
+    result = build_campaign_calendar_and_journal(transcript)
+
+    assert result["calendar"]["source"] == "base_runtime"
+    assert result["journal"]["source"] == "base_runtime"
+    assert result["journal"]["entries"][0]["text"] == "Base runtime journal entry."
5C. API/session sanity test

If you have an API sanity suite, add a test around session turn. File may be:

src/tests/api/sanity/test_rpg_session_sanity.py

Add:

def test_rpg_turn_updates_campaign_journal(client):
    start = client.post("/api/rpg/session/start", json={})
    assert start.status_code == 200
    session_id = start.json()["session_id"]

    for i in range(4):
        response = client.post(
            "/api/rpg/session/turn",
            json={"session_id": session_id, "player_input": f"I wait and observe {i}"},
        )
        assert response.status_code == 200

    get_response = client.post("/api/rpg/session/get", json={"session_id": session_id})
    assert get_response.status_code == 200
    session = get_response.json()["session"]
    runtime = session["runtime_state"]

    assert "campaign_calendar" in runtime
    assert "player_journal" in runtime
    assert len(runtime["player_journal"]["entries"]) >= 1

Adjust endpoint names/payloads if your actual API differs.

6. Update autoplay to use base journal during turns

If autoplay uses the same base session turn endpoint/runtime, this may happen automatically.

If not, patch src/tests/rpg/autoplay_llm_campaign.py after each turn result is available:

+from app.rpg.campaign_journal import advance_campaign_journal_for_turn

Then after turn_result is produced and before record = { ... }:

+        session_obj = _safe_dict(turn_result.get("session"))
+        runtime_state = _safe_dict(session_obj.get("runtime_state") or turn_result.get("runtime_state"))
+        runtime_state = advance_campaign_journal_for_turn(
+            runtime_state=runtime_state,
+            turn_index=turn_index,
+            player_input=player_action,
+            turn_contract=turn_result.get("turn_contract") or {},
+            turn_result=turn_result,
+            minutes_per_turn=int(args.campaign_minutes_per_turn),
+            journal_every_turns=int(args.journal_every_turns),
+        )
+        if session_obj:
+            session_obj["runtime_state"] = runtime_state
+            turn_result["session"] = session_obj
+        turn_result["runtime_state"] = runtime_state

Also put the same runtime state into the record:

         record = {
             ...
+            "runtime_state": runtime_state,
         }

This lets report_sections prefer source: base_runtime.

7. Validation

Compile:

python -m py_compile src\app\rpg\campaign_journal.py src\tests\rpg\autoplay\report_sections.py src\tests\rpg\autoplay_llm_campaign.py

Run focused tests:

python -m pytest src\tests\rpg\test_campaign_journal.py src\tests\rpg\autoplay\test_report_sections.py -q

Run API sanity if added:

python -m pytest src\tests\api\sanity\test_rpg_session_sanity.py -q