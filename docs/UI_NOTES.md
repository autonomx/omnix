# UI Notes

This note captures a future card polish target.

## Current behavior

- Settings shows current state.
- Settings can run a smoke check.
- The agent mode remains opt-in.
- The normal chat path remains the default.

## Desired polish

- Keep status and check output in the same card.
- Avoid creating extra conversation records from Settings checks.
- Show clear error text when a check fails.
- Make backend selection visible in responses.

## Acceptance

A future code slice should update card behavior once connector writes for that UI change are accepted.
