# Phase 17 PR Notes

This slice starts runtime hardening by wiring the Phase 16 audit into autoplay report surfaces.

## Changes

- Adds a runtime integration report adapter.
- Decorates transcript rows with per-turn integration reports.
- Aggregates ready-turn and issue counts in autoplay summaries.
- Adds an autoplay fragment wrapper that persists decorated summary/transcript JSON artifacts.
- Updates stale Phase 16 verification docs and the main parity roadmap.

## Verification

Pending GitHub Actions for the Phase 17 PR.
