# Hermes Demo Step

Phase 18 keeps the first changeable Hermes step deliberately constrained.

The candidate step is a local preview-only note update with these rules:

- It must not touch files, shell commands, GitHub, OS state, or model settings.
- It must show a before value and an after value.
- It must remain behind the review surface.
- Unknown names remain blocked.
- The result should be marked preview-only until a later code slice can safely wire the route.
