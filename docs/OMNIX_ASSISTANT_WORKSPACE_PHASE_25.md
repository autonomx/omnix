# Omnix Assistant Workspace Phase 25 — Capability Registry

Phase 25 adds capability registry contracts.

## Scope

- Model capability definitions and scopes.
- Select enabled capabilities.
- Derive whether a capability is usable in a requested scope.

## Acceptance criteria

- Disabled capabilities are excluded from active lists.
- Global capabilities are usable everywhere.
- Scoped capabilities are limited to their matching scope.
