name: Trading strategy evolution sweep

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/trading-strategy-evolution-sweep.yml"
      - "scripts/validate_trading_strategy_failed_selloff_v5_unseen.py"

permissions:
  contents: read

jobs:
  strategy-evolution:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONPATH: "src:."
      OMNIX_PERSISTENCE_MODE: legacy_test
      OMNIX_ALLOW_LEGACY_TEST_PERSISTENCE: "1"
    steps:
      - name: Check out exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install backtest dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install fastapi httpx pydantic requests python-multipart pillow websockets "psycopg[binary]>=3.2.1,<4" "psycopg-pool>=3.2.1,<4"

      - name: Restore immutable May/June validation datasets
        uses: actions/cache/restore@v4
        with:
          path: .cache/trading-liquidity-datasets
          key: trading-liquidity-datasets-v1-${{ runner.os }}-v5-unseen-${{ github.run_id }}
          restore-keys: |
            trading-liquidity-datasets-v1-${{ runner.os }}-extended-
            trading-liquidity-datasets-v1-${{ runner.os }}-

      - name: Verify untouched validation cache
        shell: bash
        run: |
          count="$(find .cache/trading-liquidity-datasets -type f -name 'session-*.json' 2>/dev/null | wc -l | tr -d ' ')"
          echo "Frozen session datasets available: ${count}"
          if [[ "${count}" -lt 58 ]]; then
            echo "::error::Expected the 58-session cache including 18 untouched May/June sessions; refusing to call providers."
            exit 2
          fi

      - name: Validate frozen V5 on untouched May/June sessions
        shell: bash
        run: |
          python scripts/validate_trading_strategy_failed_selloff_v5_unseen.py \
            --start-date 2026-05-26 \
            --end-date 2026-06-18 \
            --dataset-cache-dir .cache/trading-liquidity-datasets \
            --output-dir artifacts/failed-selloff-v5-unseen \
            --initial-cash 100000 \
            --assumed-spread-bps 40

      - name: Publish V5 untouched summary
        if: always()
        shell: bash
        run: |
          if [[ -f artifacts/failed-selloff-v5-unseen/summary.md ]]; then
            cat artifacts/failed-selloff-v5-unseen/summary.md >> "${GITHUB_STEP_SUMMARY}"
          fi

      - name: Upload V5 untouched evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trading-strategy-failed-selloff-v5-unseen
          path: artifacts/failed-selloff-v5-unseen
          if-no-files-found: warn
          retention-days: 30
