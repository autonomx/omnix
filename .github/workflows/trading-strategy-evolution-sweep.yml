name: Trading strategy evolution sweep

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/trading-strategy-evolution-sweep.yml"
      - "scripts/run_trading_strategy_failed_selloff_v9_entry_window.py"

permissions:
  contents: read

jobs:
  strategy-evolution:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      PYTHONPATH: "src:."
      OMNIX_PERSISTENCE_MODE: legacy_test
      OMNIX_ALLOW_LEGACY_TEST_PERSISTENCE: "1"
    steps:
      - name: Check out exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install backtest dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install fastapi httpx pydantic requests python-multipart pillow websockets "psycopg[binary]>=3.2.1,<4" "psycopg-pool>=3.2.1,<4"

      - name: Restore revealed development datasets
        uses: actions/cache/restore@v4
        with:
          path: .cache/trading-liquidity-datasets
          key: trading-liquidity-datasets-v1-${{ runner.os }}-v9-entry-window-${{ github.run_id }}
          restore-keys: |
            trading-liquidity-datasets-v1-${{ runner.os }}-extended-
            trading-liquidity-datasets-v1-${{ runner.os }}-

      - name: Verify revealed development cache
        shell: bash
        run: |
          count="$(find .cache/trading-liquidity-datasets -type f -name 'session-*.json' 2>/dev/null | wc -l | tr -d ' ')"
          echo "Frozen session datasets available: ${count}"
          if [[ "${count}" -lt 58 ]]; then
            echo "::error::Expected at least 58 revealed development sessions; this workflow is cache-only."
            exit 2
          fi

      - name: Run V9 entry-window restoration research
        shell: bash
        run: |
          python scripts/run_trading_strategy_failed_selloff_v9_entry_window.py \
            --dataset-cache-dir .cache/trading-liquidity-datasets \
            --output-dir artifacts/failed-selloff-v9-entry-window \
            --initial-cash 100000 \
            --assumed-spread-bps 40

      - name: Publish V9 summary
        if: always()
        shell: bash
        run: |
          if [[ -f artifacts/failed-selloff-v9-entry-window/summary.md ]]; then
            cat artifacts/failed-selloff-v9-entry-window/summary.md >> "${GITHUB_STEP_SUMMARY}"
          fi

      - name: Upload V9 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trading-strategy-failed-selloff-v9-entry-window
          path: artifacts/failed-selloff-v9-entry-window
          if-no-files-found: warn
          retention-days: 30
