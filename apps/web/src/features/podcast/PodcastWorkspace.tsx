// @ts-nocheck
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useEffect, useMemo, useRef, useState } from 'react';
import { omnixApiClient, type AssetListResponse, type JobRecord } from '../../api/client';
import type { OmnixModuleDefinition } from '../../app/modules';
import { OmnixStatusPill, WorkspacePanel } from '../../design/primitives';
import { mockPodcastSpeakerProfiles } from '../conversation-production/speakers';
import { FeatureSubmitFeedback, FeatureValidationMessage } from '../shared/FeatureSubmitFeedback';
import { buildConversationalPodcastSegments } from './scriptBuilder';
import type { PodcastFormat } from './types';
import './PodcastWorkspace.css';
import './PodcastWorkspaceLayoutFix.css';

type VoiceAsset = AssetListResponse['assets'][number];
type SpeakerDraft = ReturnType<typeof toSpeakerDraft>;
type TranscriptRow = { timestamp: string; speaker: string; text: string };
type PlayablePodcastOutput = { dataUrl: string; duration: number; jobId: string; key: string; title: string; live?: boolean };
type VoiceAssignment = { voiceId: string | null; style: string };

const defaultTitle = 'The Future of AI in Everyday Life';
const defaultBrief = 'Explore how artificial intelligence is shaping our daily lives, transforming work and productivity, inspiring creativity, influencing relationships, and augmenting decision-making. We will discuss opportunities, risks, and what comes next.';
const durationOptions = ['2 min', '5 min', '10 min', '15 min', '20 min', '30 min', '45 min', '60 min'];
const formatOptions: Array<{ id: PodcastFormat; label: string; description: string }> = [
  { id: 'debate', label: 'Debate', description: 'Two or more opposing sides' },
  { id: 'interview', label: 'Interview', description: 'Host interviews guests' },
  { id: 'speech', label: 'Speech', description: 'Solo host presentation' },
];
const terminalStatuses = ['completed', 'complete', 'succeeded', 'success', 'done', 'failed', 'error', 'cancelled', 'canceled'];
const outputSettings = { speed: 1, pitch: 0, stability: 0.72, similarity: 0.78 };
const audioEffects = ['Compression', 'De-esser'];
const transcriptStorageKey = 'omnix:persistent-podcast-transcripts:v2';
const stitchedLiveKey = 'live:stitched-preview';
const wordsPerMinute = 150;
const seamTrimSeconds = 0.08;
const seamFadeSeconds = 0.006;
const restoreSafetySeconds = 0.02;

function toSpeakerDraft(profile: (typeof mockPodcastSpeakerProfiles)[number]) { return { id: profile.id, name: profile.name, role: profile.role, avatar: profile.avatar, identity: profile.identity, beliefs: profile.beliefs.join(', '), personality: profile.personality.join(', '), speakingStyle: profile.speakingStyle.join(', '), goal: profile.segmentGoals.map(({ goal }) => goal).join(' -> ') || profile.defaultGoal, instructions: '', voice: '' }; }
function splitTags(value: string): string[] { return String(value || '').split(',').map((tag) => tag.trim()).filter(Boolean); }
function durationMinutes(duration: string): number { return Math.max(1, Number.parseInt(duration, 10) || 1); }
function durationSeconds(duration: string): number { return durationMinutes(duration) * 60; }
function targetWordCount(duration: string): number { return Math.max(220, Math.round(durationMinutes(duration) * wordsPerMinute)); }
function formatClock(seconds: number): string { const safe = Number.isFinite(seconds) && seconds > 0 ? Math.floor(seconds) : 0; return `${String(Math.floor(safe / 60)).padStart(2, '0')}:${String(safe % 60).padStart(2, '0')}`; }
function isTerminal(status: unknown): boolean { return terminalStatuses.includes(String(status ?? '').toLowerCase()); }
function isFailed(status: unknown): boolean { return ['failed', 'error', 'cancelled', 'canceled'].includes(String(status ?? '').toLowerCase()); }
function firstTag(value: string): string { return splitTags(value)[0] ?? 'Neutral'; }
function voiceStoragePath(asset: VoiceAsset | undefined): string { return typeof (asset as any)?.storage_path === 'string' ? (asset as any).storage_path : ''; }
function voiceAssetId(asset: VoiceAsset | undefined): string { return typeof (asset as any)?.id === 'string' ? (asset as any).id : ''; }
function voiceAssetName(asset: VoiceAsset): string { const metadata = (asset as any).metadata ?? {}; const named = typeof metadata.profile_name === 'string' ? metadata.profile_name : typeof metadata.name === 'string' ? metadata.name : ''; if (named.trim()) return named.trim(); const source = voiceStoragePath(asset) || voiceAssetId(asset); return source.split(/[\\/]/).pop()?.replace(/\.[^.]+$/, '') || voiceAssetId(asset) || 'Voice'; }
function voiceOptionsFromAssets(assets: VoiceAsset[]) { return assets.filter((asset) => asset.type === 'voice_profile').map((asset) => ({ id: voiceStoragePath(asset) || voiceAssetId(asset), label: voiceAssetName(asset) })).filter((voice) => voice.id); }
function jobTitle(job: { type: string; input_payload?: unknown }): string { const payload = job.input_payload as any; return payload && typeof payload.title === 'string' ? payload.title : job.type; }
function delay(ms: number) { return new Promise((resolve) => setTimeout(resolve, ms)); }
function speakerDisplayName(speaker: string): string { return speaker.length > 18 ? `${speaker.slice(0, 17)}…` : speaker; }
function safeDownloadName(value: string): string { return value.replace(/[^A-Za-z0-9_.-]+/g, '-').replace(/^-+|-+$/g, '') || 'podcast-output'; }
function readStoredTranscripts(): Record<string, TranscriptRow[]> { if (typeof window === 'undefined') return {}; try { return JSON.parse(window.localStorage.getItem(transcriptStorageKey) || '{}') || {}; } catch { return {}; } }
function writeStoredTranscripts(value: Record<string, TranscriptRow[]>): void { if (typeof window === 'undefined') return; try { window.localStorage.setItem(transcriptStorageKey, JSON.stringify(value)); } catch {} }
function transcriptRowsFromSegments(segments: Array<{ speaker: string; text: string }>, targetSeconds: number): TranscriptRow[] { if (!segments.length) return []; const step = Math.max(8, targetSeconds / segments.length); return segments.map((segment, index) => ({ timestamp: formatClock(index * step), speaker: String(segment.speaker || 'Speaker'), text: String(segment.text || '') })); }
function transcriptRowsFromJob(job: JobRecord | undefined): TranscriptRow[] { const payload = job?.input_payload as any; const scriptSegments = Array.isArray(payload?.script_segments) ? payload.script_segments : []; if (!scriptSegments.length) return []; return transcriptRowsFromSegments(scriptSegments.map((segment) => ({ speaker: String(segment.speaker || 'Speaker'), text: String(segment.text || '') })), Number(payload?.constraints?.targetDurationSeconds || payload?.target_duration_seconds || 0)); }
function speakerCounts(segments: Array<{ speaker: string }>) { const counts: Record<string, number> = {}; for (const segment of segments) counts[segment.speaker] = (counts[segment.speaker] ?? 0) + 1; return counts; }
function defaultVoicesFromOptions(voiceOptions: Array<{ id: string; label: string }>) { return { narrator: voiceOptions[0]?.id || null, female: voiceOptions[1]?.id || voiceOptions[0]?.id || null, male: voiceOptions[2]?.id || voiceOptions[0]?.id || null }; }
function stageState(status: unknown, index: number, activeIndex: number) { const normalized = String(status ?? '').toLowerCase(); if (isFailed(normalized)) return 'failed'; if (['completed', 'complete', 'succeeded', 'success', 'done'].includes(normalized)) return 'done'; if (['running', 'in_progress', 'active', 'processing', 'leased', 'retrying', 'queued'].includes(normalized)) return 'active'; return index === activeIndex ? 'active' : index < activeIndex ? 'done' : 'pending'; }
function podcastStages() { return [{ id: 'producer_plan', label: 'Producer Plan', resource_class: 'cpu' as const, status: 'queued' as const }, { id: 'performance_script', label: 'Performance Script', resource_class: 'cpu' as const, status: 'queued' as const }, { id: 'speaking_turns', label: 'Speaking Turns', resource_class: 'gpu:tts' as const, status: 'queued' as const }, { id: 'mix', label: 'Mix', resource_class: 'cpu' as const, status: 'queued' as const }, { id: 'podcast_renderer', label: 'Podcast Renderer', resource_class: 'cpu' as const, status: 'queued' as const }]; }

function extractPlayableOutputs(jobs: Array<JobRecord | undefined>, extra: PlayablePodcastOutput[] = []): PlayablePodcastOutput[] {
  const outputs = [...extra];
  const seen = new Set(outputs.map((output) => output.key));
  for (const job of jobs) for (const ref of ((job?.output_refs ?? []) as any[])) {
    const dataUrl = typeof ref.data_url === 'string' ? ref.data_url : typeof ref.audio_url === 'string' ? ref.audio_url : '';
    if (!dataUrl.startsWith('data:audio/') && !dataUrl.startsWith('blob:') && !dataUrl.startsWith('/api/')) continue;
    const title = typeof ref.title === 'string' && ref.title.trim() ? ref.title : jobTitle(job as JobRecord);
    const key = `${job?.id ?? 'job'}:${String(ref.asset_id || title || outputs.length)}`;
    if (!seen.has(key)) { seen.add(key); outputs.push({ dataUrl, duration: Number(ref.duration || 0), jobId: job?.id || 'job', key, title }); }
  }
  return outputs;
}
async function waitForPlayableJob(job: JobRecord, timeoutMs = 120_000): Promise<JobRecord> { if (extractPlayableOutputs([job]).length || isTerminal(job.status)) return job; const startedAt = Date.now(); let latest = job; while (Date.now() - startedAt < timeoutMs) { await delay(1_250); try { latest = await omnixApiClient.getJob(job.id); if (extractPlayableOutputs([latest]).length || isTerminal(latest.status)) return latest; } catch {} } return latest; }
async function arrayBufferFromAudioUrl(url: string): Promise<ArrayBuffer> { if (url.startsWith('data:')) { const [, payload = ''] = url.split(',', 2); const isBase64 = /^data:[^,]+;base64,/i.test(url); const binary = isBase64 ? atob(payload) : decodeURIComponent(payload); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i += 1) bytes[i] = binary.charCodeAt(i) & 0xff; return bytes.buffer; } const response = await fetch(url); return response.arrayBuffer(); }
function encodeWav(samples: Float32Array, sampleRate: number): Blob { const dataSize = samples.length * 2; const buffer = new ArrayBuffer(44 + dataSize); const view = new DataView(buffer); const text = (offset: number, value: string) => { for (let i = 0; i < value.length; i += 1) view.setUint8(offset + i, value.charCodeAt(i)); }; text(0, 'RIFF'); view.setUint32(4, 36 + dataSize, true); text(8, 'WAVE'); text(12, 'fmt '); view.setUint32(16, 16, true); view.setUint16(20, 1, true); view.setUint16(22, 1, true); view.setUint32(24, sampleRate, true); view.setUint32(28, sampleRate * 2, true); view.setUint16(32, 2, true); view.setUint16(34, 16, true); text(36, 'data'); view.setUint32(40, dataSize, true); let offset = 44; for (let i = 0; i < samples.length; i += 1, offset += 2) { const sample = Math.max(-1, Math.min(1, samples[i])); view.setInt16(offset, sample < 0 ? sample * 0x8000 : sample * 0x7fff, true); } return new Blob([buffer], { type: 'audio/wav' }); }
function copyMono(source: AudioBuffer, destination: Float32Array, offset: number, sampleRate: number, trimStart = 0, fadeIn = 0) { const sourceData = source.getChannelData(0); const targetLength = Math.max(1, Math.round(source.duration * sampleRate)); const copiedLength = Math.max(1, targetLength - trimStart); const ratio = source.sampleRate / sampleRate; for (let i = 0; i < copiedLength; i += 1) { const sourceIndex = source.sampleRate === sampleRate ? i + trimStart : (i + trimStart) * ratio; const before = Math.floor(sourceIndex); const after = Math.min(before + 1, sourceData.length - 1); const fraction = sourceIndex - before; const gain = fadeIn > 0 && i < fadeIn ? i / fadeIn : 1; destination[offset + i] = (sourceData[before] * (1 - fraction) + sourceData[after] * fraction) * gain; } return copiedLength; }
async function stitchLiveOutputs(outputs: PlayablePodcastOutput[]): Promise<PlayablePodcastOutput | null> { if (!outputs.length) return null; const title = `Live preview stitched ${outputs.length} / ${outputs.length}`; const fallback = { ...outputs[outputs.length - 1], key: stitchedLiveKey, title, live: true, duration: outputs.reduce((sum, output) => sum + Number(output.duration || 0), 0) || outputs[outputs.length - 1].duration }; if (outputs.length === 1 || typeof window === 'undefined') return fallback; const AudioContextCtor = (window as any).AudioContext || (window as any).webkitAudioContext; if (!AudioContextCtor || !URL?.createObjectURL) return fallback; const context = new AudioContextCtor(); try { const buffers = await Promise.all(outputs.map(async (output) => context.decodeAudioData(await arrayBufferFromAudioUrl(output.dataUrl)))); const sampleRate = buffers[0]?.sampleRate || 24000; const trimSamples = buffers.map((buffer, index) => index === 0 ? 0 : Math.min(Math.round(seamTrimSeconds * sampleRate), Math.max(0, Math.round(buffer.duration * sampleRate) - 1))); const fadeSamples = Math.max(1, Math.round(seamFadeSeconds * sampleRate)); const totalLength = buffers.reduce((sum, buffer, index) => sum + Math.max(1, Math.round(buffer.duration * sampleRate) - trimSamples[index]), 0); const stitched = new Float32Array(totalLength); let offset = 0; buffers.forEach((buffer, index) => { offset += copyMono(buffer, stitched, offset, sampleRate, trimSamples[index], index === 0 ? 0 : fadeSamples); }); const blob = encodeWav(stitched, sampleRate); return { dataUrl: URL.createObjectURL(blob), duration: stitched.length / sampleRate, jobId: 'live-preview', key: stitchedLiveKey, title, live: true }; } catch { return fallback; } finally { try { await context.close(); } catch {} } }

function textFromChatResponse(response: any): string { const direct = [response?.content, response?.text, response?.response, response?.reply, response?.message?.content, response?.assistant?.content, response?.assistant_message?.content, response?.output].find((value) => typeof value === 'string' && value.trim()); if (direct) return String(direct); const messages = Array.isArray(response?.messages) ? response.messages : Array.isArray(response?.session?.messages) ? response.session.messages : []; return [...messages].reverse().find((message) => String(message?.role || '').toLowerCase() === 'assistant' && typeof message?.content === 'string')?.content || ''; }
function parseSegmentsFromLlm(raw: string): Array<{ index: number; speaker: string; text: string }> { const stripped = raw.replace(/```(?:json)?/gi, '').replace(/```/g, '').trim(); const jsonMatch = stripped.match(/\{[\s\S]*\}/) || stripped.match(/\[[\s\S]*\]/); if (jsonMatch) { try { const parsed = JSON.parse(jsonMatch[0]); const rawSegments = Array.isArray(parsed) ? parsed : Array.isArray(parsed?.segments) ? parsed.segments : []; const segments = rawSegments.map((segment: any, index: number) => ({ index, speaker: String(segment?.speaker || segment?.name || 'Speaker').trim(), text: String(segment?.text || segment?.line || segment?.content || '').trim() })).filter((segment) => segment.speaker && segment.text); if (segments.length) return segments; } catch {} } return stripped.split('\n').map((line, index) => { const match = line.match(/^\s*([^:]{2,40})\s*:\s*(.+)$/); return match ? { index, speaker: match[1].trim(), text: match[2].trim() } : null; }).filter(Boolean) as Array<{ index: number; speaker: string; text: string }>; }
async function generateLlmPodcastSegments(args: { title: string; brief: string; audience: string; duration: string; format: PodcastFormat; speakers: SpeakerDraft[]; tone: string; language: string }) { const targetLines = Math.min(48, Math.max(args.speakers.length * 4, Math.ceil(targetWordCount(args.duration) / 70))); const validNames = args.speakers.map((speaker) => speaker.name).join(', '); const speakerList = args.speakers.map((speaker) => `${speaker.name} (${speaker.role}): identity=${speaker.identity}; beliefs=${speaker.beliefs}; personality=${speaker.personality}; speaking_style=${speaker.speakingStyle}; goal=${speaker.goal}; instructions=${speaker.instructions || 'none'}`).join('\n'); const prompt = `Generate a real, speaker-tagged podcast script. Return JSON only with this shape: {"segments":[{"speaker":"${args.speakers[0]?.name || 'Host'}","text":"spoken line"}]}.
Topic: ${args.title}
Brief: ${args.brief}
Audience: ${args.audience}
Format: ${args.format}
Tone: ${args.tone}
Language: ${args.language}
Target lines: ${targetLines}
Valid speaker names: ${validNames}
Speakers, use these exact names only:
${speakerList}`; try { const session = await omnixApiClient.createChatSession({ title: `Podcast script: ${args.title}`.slice(0, 64), system_prompt: 'You are a podcast scriptwriter. Return only valid JSON requested by the user.' }); const response = await omnixApiClient.sendChatMessage(session.id, { content: prompt }); const rows = parseSegmentsFromLlm(textFromChatResponse(response)); if (rows.length >= Math.min(2, args.speakers.length)) return rows.map((segment, index) => ({ ...segment, index })); } catch {} return buildConversationalPodcastSegments(args.title, args.brief, args.audience, args.speakers, args.duration).map((segment, index) => ({ index, speaker: segment.speaker, text: segment.text })); }
function makeAssignments(segments: Array<{ speaker: string }>, speakers: SpeakerDraft[], voiceOptions: Array<{ id: string; label: string }>): Record<string, VoiceAssignment> { const names = [...new Set(segments.map((segment) => segment.speaker))]; return Object.fromEntries(names.map((name, index) => { const speaker = speakers.find((entry) => entry.name === name || entry.role === name) || speakers[index % Math.max(speakers.length, 1)]; return [name, { voiceId: speaker?.voice || voiceOptions[index % Math.max(voiceOptions.length, 1)]?.id || null, style: firstTag(speaker?.speakingStyle || speaker?.personality || name) }]; })); }
function buildSingleSpeakerJobPayload(args: { title: string; language: string; segment: { index: number; speaker: string; text: string }; voiceOptions: Array<{ id: string; label: string }>; voiceAssignments: Record<string, VoiceAssignment> }) { const assignment = args.voiceAssignments[args.segment.speaker]; const voiceId = assignment?.voiceId || args.voiceOptions[0]?.id || null; return { title: `${args.title} - ${args.segment.speaker} ${args.segment.index + 1}`, text: args.segment.text, provider_id: null, speaker: args.segment.speaker, voice_id: voiceId, language: args.language, script_mode: 'single_speaker', script_speakers: [{ name: args.segment.speaker, count: 1 }], script_segments: [args.segment], character_voice_assignments: [{ speaker: args.segment.speaker, voice_id: voiceId, style: assignment?.style || 'Neutral', line_count: 1 }], voice_mapping: { [args.segment.speaker]: voiceId }, default_voices: defaultVoicesFromOptions(args.voiceOptions), output_settings: outputSettings, audio_effects: audioEffects, save_output: true, renderer: 'podcast-live-preview' }; }
function buildPodcastJobPayload(args: any) { const counts = speakerCounts(args.segments); const targetSeconds = durationSeconds(args.duration); const names = Object.keys(counts); const voiceMapping = Object.fromEntries(names.map((name) => [name, args.voiceAssignments[name]?.voiceId || null])); return { title: args.title, brief: args.brief, format: args.format, audience: args.audience, duration_minutes: durationMinutes(args.duration), target_duration_seconds: targetSeconds, target_word_count: targetWordCount(args.duration), tone: args.tone, language: args.language, generation_style: args.generationStyle, review_policy: { mode: args.generationStyle }, renderer: 'podcast', text: args.segments.map((segment) => `${segment.speaker}: ${segment.text}`).join('\n'), provider_id: null, speaker: names[0] || args.speakers[0]?.name || 'Host', voice_id: voiceMapping[names[0]] || args.voiceOptions[0]?.id || null, script_mode: args.segments.length > 1 ? 'multi_speaker' : 'single_speaker', script_speakers: names.map((name) => ({ name, count: counts[name] })), script_segments: args.segments, character_voice_assignments: names.map((name) => ({ speaker: name, voice_id: voiceMapping[name] || null, style: args.voiceAssignments[name]?.style || 'Neutral', line_count: counts[name] ?? 0 })), voice_mapping: voiceMapping, default_voices: defaultVoicesFromOptions(args.voiceOptions), output_settings: outputSettings, audio_effects: audioEffects, save_output: true, speakers: args.speakers.map((speaker) => ({ id: speaker.id, name: speaker.name, role: speaker.role, speakerInstructions: speaker.instructions, voiceMapping: { speakerId: speaker.id, voiceId: speaker.voice } })), constraints: { maxSpeakerTurnSeconds: 45 } }; }

export function PodcastWorkspace({ module }: { module: OmnixModuleDefinition }) {
  const queryClient = useQueryClient();
  const audioRef = useRef<HTMLAudioElement | null>(null);
  const currentOutputRef = useRef<PlayablePodcastOutput | null>(null);
  const pendingOutputRef = useRef<PlayablePodcastOutput | null>(null);
  const stitchVersionRef = useRef(0);
  const autoplayStartedRef = useRef(false);
  const userPausedRef = useRef(false);
  const restoreRef = useRef<{ time: number; play: boolean } | null>(null);
  const jobsQuery = useQuery({ queryKey: ['platform', 'jobs'], queryFn: async () => { try { return await omnixApiClient.listJobs(); } catch { return { jobs: [] }; } }, retry: false, refetchInterval: false, refetchOnWindowFocus: false });
  const assetsQuery = useQuery({ queryKey: ['platform', 'assets'], queryFn: async () => { try { return await omnixApiClient.listAssets(); } catch { return { assets: [] }; } }, retry: false, refetchInterval: false, refetchOnWindowFocus: false });
  const [title, setTitle] = useState(defaultTitle); const [brief, setBrief] = useState(defaultBrief); const [audience, setAudience] = useState('Software Engineers'); const [duration, setDuration] = useState('20 min'); const [tone, setTone] = useState('Professional'); const [language, setLanguage] = useState('English (US)');
  const [format, setFormat] = useState<PodcastFormat>('debate'); const [generationStyle, setGenerationStyle] = useState('automatic'); const [speakers, setSpeakers] = useState(() => mockPodcastSpeakerProfiles.map(toSpeakerDraft)); const [transcript, setTranscript] = useState<TranscriptRow[]>([]); const [storedTranscripts, setStoredTranscripts] = useState<Record<string, TranscriptRow[]>>(() => readStoredTranscripts());
  const [directorNote, setDirectorNote] = useState('No live production is running. Configure the episode, then press Generate live podcast.'); const [liveCommand, setLiveCommand] = useState(''); const [playbackRate, setPlaybackRate] = useState('1.0x'); const [selectedOutputKey, setSelectedOutputKey] = useState(''); const [livePieces, setLivePieces] = useState<PlayablePodcastOutput[]>([]); const [currentOutput, setCurrentOutput] = useState<PlayablePodcastOutput | null>(null); const [liveAutoplay, setLiveAutoplay] = useState(false); const [actionMessage, setActionMessage] = useState('Ready for automatic production.');
  const voiceOptions = useMemo(() => voiceOptionsFromAssets(assetsQuery.data?.assets ?? []), [assetsQuery.data?.assets]); const podcastJobs = jobsQuery.data?.jobs.filter((job) => job.module === 'podcast') ?? [];
  function installOutput(output: PlayablePodcastOutput, options: { restoreTime?: number; shouldPlay?: boolean } = {}) { restoreRef.current = { time: options.restoreTime ?? audioRef.current?.currentTime ?? 0, play: Boolean(options.shouldPlay) }; currentOutputRef.current = output; setCurrentOutput(output); setSelectedOutputKey(output.key); }
  function maybeInstallStitched(stitched: PlayablePodcastOutput) { const audio = audioRef.current; if (currentOutputRef.current?.live && audio && !audio.paused && !audio.ended) { pendingOutputRef.current = stitched; return; } pendingOutputRef.current = null; installOutput(stitched, { restoreTime: audio?.currentTime || 0, shouldPlay: false }); }
  async function rebuildStitchedPreview(outputs: PlayablePodcastOutput[]) { const version = ++stitchVersionRef.current; const stitched = await stitchLiveOutputs(outputs); if (!stitched || version !== stitchVersionRef.current) return; maybeInstallStitched(stitched); }
  function queueLiveOutput(output: PlayablePodcastOutput) { setLivePieces((current) => { const next = [...current, output]; void rebuildStitchedPreview(next); return next; }); if (!selectedOutputKey) setSelectedOutputKey(stitchedLiveKey); }
  function resetPreview() { try { if (audioRef.current?.getAttribute('src')) audioRef.current.pause(); } catch {} pendingOutputRef.current = null; stitchVersionRef.current += 1; autoplayStartedRef.current = false; userPausedRef.current = false; currentOutputRef.current = null; setCurrentOutput(null); setLivePieces([]); }
  const createJobMutation = useMutation({ mutationFn: async () => { const segments = await generateLlmPodcastSegments({ title, brief, audience, duration, format, speakers, tone, language }); const rows = transcriptRowsFromSegments(segments, durationSeconds(duration)); const voiceAssignments = makeAssignments(segments, speakers, voiceOptions); setTranscript(rows); setLiveAutoplay(true); setSelectedOutputKey('__streaming__'); setDirectorNote('Script is ready. The first preview can auto-start; later chunks respect pause and extend one continuous stitched timeline.'); for (const segment of segments) { const segmentJob = await omnixApiClient.createJob({ module: 'podcast', type: 'tts.synthesize', resource_class: 'gpu:tts', priority: 1, input_payload: buildSingleSpeakerJobPayload({ title, language, segment, voiceOptions, voiceAssignments }), stages: podcastStages().slice(0, 2) }, { timeoutMs: 120000, timeoutMessage: 'Podcast live preview turn is still running.' }); const completed = await waitForPlayableJob(segmentJob); const output = extractPlayableOutputs([completed])[0]; if (output) { queueLiveOutput({ ...output, key: `live:${segment.index}:${output.key}`, title: `${segment.speaker}: voice mapped`, live: true }); setActionMessage(`Stitched live preview ${segment.index + 1}/${segments.length}: ${segment.speaker}`); } } const finalJob = await omnixApiClient.createJob({ module: 'podcast', type: 'tts.multi_speaker_synthesize', resource_class: 'gpu:tts', priority: 0, input_payload: buildPodcastJobPayload({ title, brief, format, audience, duration, tone, language, generationStyle, speakers, voiceOptions, voiceAssignments, segments }), stages: podcastStages() }, { timeoutMs: 180000, timeoutMessage: 'Final podcast render is still running.' }); const completedFinalJob = await waitForPlayableJob(finalJob, 180000); if (rows.length) setStoredTranscripts((current) => { const next = { ...current, [completedFinalJob.id]: rows }; writeStoredTranscripts(next); return next; }); return completedFinalJob; }, onMutate: () => { resetPreview(); setTranscript([]); setLiveAutoplay(false); setSelectedOutputKey('__script__'); setDirectorNote('Generating a real speaker-tagged script with the configured chat provider.'); setActionMessage('Requesting podcast script from LLM...'); }, onSuccess: async (job) => { const output = extractPlayableOutputs([job])[0]; setDirectorNote(output ? 'Final podcast audio is ready. The stitched live preview stays active until you select the final render.' : 'Podcast render completed but no playable audio output was attached.'); setActionMessage(output ? `Podcast audio ready: ${job.id}` : `Podcast production completed without playable audio: ${job.id}`); await Promise.all([queryClient.invalidateQueries({ queryKey: ['platform', 'jobs'] }), queryClient.invalidateQueries({ queryKey: ['platform', 'assets'] })]); }, onError: (error) => { setDirectorNote(error instanceof Error ? error.message : 'Podcast request failed.'); setActionMessage(error instanceof Error ? error.message : 'Podcast request failed.'); setLiveAutoplay(false); } });
  const activeJob = podcastJobs.find((job) => !isTerminal(job.status)); const connectedJob = selectedOutputKey === '__new__' ? undefined : (createJobMutation.data as JobRecord | undefined) ?? activeJob ?? podcastJobs[0]; const persistedTranscript = connectedJob?.id ? storedTranscripts[connectedJob.id] ?? [] : []; const transcriptRows = selectedOutputKey === '__new__' ? [] : transcript.length ? transcript : persistedTranscript.length ? persistedTranscript : transcriptRowsFromJob(connectedJob); const liveActive = createJobMutation.isPending || Boolean(activeJob && !isTerminal(activeJob.status)); const liveStatus = createJobMutation.isPending ? (selectedOutputKey === '__script__' ? 'SCRIPTING' : 'STREAMING') : connectedJob ? String(connectedJob.status).toUpperCase() : 'IDLE'; const stages = createJobMutation.isPending ? podcastStages().map((stage, index) => ({ ...stage, state: index < 2 ? 'done' : index === 2 ? 'active' : 'pending' })) : (connectedJob?.stages?.length ? connectedJob.stages.map((stage, index) => ({ id: stage.id, label: stage.label, state: stageState(stage.status, index, 0) })) : podcastStages().map((stage) => ({ ...stage, state: 'pending' }))); const recentJobs = podcastJobs.slice(0, 6).map((job) => ({ id: job.id, name: jobTitle(job), status: job.status, duration })); const showBriefError = brief.trim().length === 0 && createJobMutation.isIdle === false;
  useEffect(() => { currentOutputRef.current = currentOutput; }, [currentOutput]); useEffect(() => { if (!voiceOptions.length) return; setSpeakers((current) => current.map((speaker, index) => speaker.voice && voiceOptions.some((voice) => voice.id === speaker.voice) ? speaker : { ...speaker, voice: voiceOptions[index % voiceOptions.length].id })); }, [voiceOptions.map((voice) => voice.id).join('|')]); useEffect(() => { const audio = audioRef.current; if (!audio || !currentOutput) return; audio.playbackRate = Number.parseFloat(playbackRate) || 1; if (!liveAutoplay || !currentOutput.live || autoplayStartedRef.current) return; autoplayStartedRef.current = true; const timer = window.setTimeout(() => { const playResult = audio.play(); if (playResult?.catch) playResult.catch(() => setActionMessage('Audio is ready. Press play if the browser blocked autoplay.')); }, 50); return () => window.clearTimeout(timer); }, [currentOutput?.key, currentOutput?.dataUrl, liveAutoplay, playbackRate]);
  function startGeneration() { if (!brief.trim()) return; createJobMutation.mutate(); } function updateSpeaker(id: string, field: string, value: string) { setSpeakers((current) => current.map((speaker) => speaker.id === id ? { ...speaker, [field]: value } : speaker)); } function addParticipant() { const next = speakers.length + 1; setSpeakers((current) => [...current, { id: `guest_${next}`, name: `Guest ${next}`, role: 'Guest Analyst', avatar: `G${next}`, identity: 'Guest Analyst', beliefs: '', personality: '', speakingStyle: '', goal: '', instructions: '', voice: voiceOptions[0]?.id ?? '' }]); } function resetPodcast() { resetPreview(); setTitle(''); setBrief(''); setDuration('5 min'); setTranscript([]); setLiveAutoplay(false); setDirectorNote('New podcast request cleared. Add a title and brief, then generate.'); setSelectedOutputKey('__new__'); setActionMessage('New podcast ready.'); } function submitLiveCommand() { const command = liveCommand.trim(); if (!command) return; setDirectorNote(`Director note: ${command}`); setTranscript((lines) => [...lines, { timestamp: formatClock(lines.length * 15), speaker: 'Director', text: command }]); setLiveCommand(''); } function selectRecentJob(jobId: string) { const job = podcastJobs.find((entry) => entry.id === jobId); const output = extractPlayableOutputs([job])[0]; const rows = storedTranscripts[jobId] ?? transcriptRowsFromJob(job); if (rows.length) setTranscript(rows); if (output) installOutput(output, { shouldPlay: false }); } function downloadCurrentOutput(label = 'Podcast audio') { if (!currentOutput || typeof document === 'undefined') return; const link = document.createElement('a'); link.href = currentOutput.dataUrl; link.download = `${safeDownloadName(currentOutput.title || title)}.wav`; link.click(); setActionMessage(`${label}: download started.`); } async function copyEpisodeLink() { const link = typeof window !== 'undefined' ? `${window.location.href.split('#')[0]}#${connectedJob?.id ?? 'podcast'}` : connectedJob?.id ?? 'podcast'; try { await navigator.clipboard?.writeText(link); setActionMessage('Podcast link copied.'); } catch { setActionMessage(`Podcast link: ${link}`); } } function maybeInstallPendingWhilePaused() { const audio = audioRef.current; if (!pendingOutputRef.current || (audio && !audio.paused && !audio.ended)) return; const pending = pendingOutputRef.current; pendingOutputRef.current = null; installOutput(pending, { restoreTime: audio?.currentTime || 0, shouldPlay: false }); } function onAudioLoadedMetadata(event: any) { const audio = event.currentTarget; audio.playbackRate = Number.parseFloat(playbackRate) || 1; const restore = restoreRef.current; if (!restore) return; restoreRef.current = null; if (restore.time > 0 && Number.isFinite(audio.duration)) audio.currentTime = Math.min(restore.time + (restore.play ? restoreSafetySeconds : 0), Math.max(0, audio.duration - 0.05)); if (restore.play) { const playResult = audio.play(); if (playResult?.catch) playResult.catch(() => setActionMessage('Audio is ready. Press play if the browser blocked autoplay.')); } } function onAudioEnded() { const audio = audioRef.current; if (pendingOutputRef.current) { const pending = pendingOutputRef.current; pendingOutputRef.current = null; installOutput(pending, { restoreTime: currentOutputRef.current?.duration || audio?.currentTime || 0, shouldPlay: !userPausedRef.current }); return; } const finalOutput = extractPlayableOutputs([createJobMutation.data, ...podcastJobs]).find((output) => !output.live); if (finalOutput) installOutput(finalOutput, { shouldPlay: false }); }
  return <WorkspacePanel className="podcast-workspace-panel"><div className="podcast-studio-shell"><header className="podcast-studio-header"><div><p className="eyebrow">Conversation engine</p><h2 id="module-title">{module.label}</h2><p>Create a real LLM-generated podcast script, stitch live preview turns into one growing stream, then keep one final render for playback and download.</p></div><code>/podcast-renderer</code></header><div className="podcast-studio-grid"><section className="podcast-studio-stack"><article className="podcast-card episode-setup-card"><div className="card-heading-row"><h3>1. Episode setup</h3><button className="ghost-button compact" type="button" onClick={resetPodcast}>New podcast</button></div><div className="episode-setup-grid"><div className="podcast-field-stack"><label>Topic / Episode title<input value={title} onChange={(event) => setTitle(event.target.value)} /></label><label>Episode brief<textarea rows={5} value={brief} onChange={(event) => setBrief(event.target.value)} /><small>{brief.length}/2000</small></label><label>Audience<select value={audience} onChange={(event) => setAudience(event.target.value)}><option>Software Engineers</option><option>General Public</option><option>Executives</option><option>Students</option><option>Experts</option></select></label></div><div className="podcast-config-stack"><span className="podcast-label">Podcast format</span><div className="format-card-grid">{formatOptions.map((option) => <button key={option.id} type="button" className={option.id === format ? 'selected' : undefined} onClick={() => setFormat(option.id)}><strong>{option.label}</strong><small>{option.description}</small></button>)}</div><div className="podcast-select-grid"><label>Duration<select value={duration} onChange={(event) => setDuration(event.target.value)}>{durationOptions.map((option) => <option key={option}>{option}</option>)}</select></label><label>Tone<select value={tone} onChange={(event) => setTone(event.target.value)}><option>Professional</option><option>Conversational</option><option>Humorous</option></select></label><label>Language<select value={language} onChange={(event) => setLanguage(event.target.value)}><option>English (US)</option><option>English (UK)</option></select></label></div><label>Generation Style<select value={generationStyle} onChange={(event) => setGenerationStyle(event.target.value)}><option value="automatic">Automatic</option><option value="guided">Guided</option></select></label></div></div></article><article className="podcast-card"><div className="card-heading-row"><h3>2. Participants and voice casting</h3><small>{voiceOptions.length ? `Loaded ${voiceOptions.length} Voice Library voice${voiceOptions.length === 1 ? '' : 's'}` : 'No Voice Library voices found'}</small></div><div className="speaker-table editable-speaker-table"><div className="speaker-row speaker-header"><span>Speaker</span><span>Identity</span><span>Voice</span><span>Beliefs</span><span>Personality</span><span>Speaking style</span><span>Goal this episode</span><span>Instructions</span></div>{speakers.map((speaker) => <div className="speaker-row editable-speaker-row" key={speaker.id}><span className="speaker-cell-main"><b className={`speaker-avatar ${speaker.id}`}>{speaker.avatar}</b><span><input value={speaker.name} onChange={(event) => updateSpeaker(speaker.id, 'name', event.target.value)} /><input value={speaker.role} onChange={(event) => updateSpeaker(speaker.id, 'role', event.target.value)} /></span></span><span><input value={speaker.identity} onChange={(event) => updateSpeaker(speaker.id, 'identity', event.target.value)} /></span><span><select aria-label={`${speaker.name} voice`} value={speaker.voice} onChange={(event) => updateSpeaker(speaker.id, 'voice', event.target.value)}>{voiceOptions.map((voice) => <option key={voice.id} value={voice.id}>{voice.label}</option>)}{!voiceOptions.length ? <option value="">No cloned voices</option> : null}</select></span><span><textarea rows={2} value={speaker.beliefs} onChange={(event) => updateSpeaker(speaker.id, 'beliefs', event.target.value)} /></span><span><textarea rows={2} value={speaker.personality} onChange={(event) => updateSpeaker(speaker.id, 'personality', event.target.value)} /></span><span><textarea rows={2} value={speaker.speakingStyle} onChange={(event) => updateSpeaker(speaker.id, 'speakingStyle', event.target.value)} /></span><span><textarea rows={2} value={speaker.goal} onChange={(event) => updateSpeaker(speaker.id, 'goal', event.target.value)} /></span><span><textarea rows={2} value={speaker.instructions} onChange={(event) => updateSpeaker(speaker.id, 'instructions', event.target.value)} /></span></div>)}</div><button className="ghost-button" type="button" onClick={addParticipant}>+ Add participant</button></article><form onSubmit={(event) => { event.preventDefault(); startGeneration(); }}><button className="podcast-generate-button" type="submit" disabled={createJobMutation.isPending}>Generate live podcast</button></form><FeatureValidationMessage show={showBriefError} message="Enter an episode brief before generating a podcast." /><FeatureSubmitFeedback error={createJobMutation.error} errorPrefix="Podcast request" isError={createJobMutation.isError} isPending={createJobMutation.isPending} jobId={createJobMutation.data?.id} pendingMessage="Generating script and stitching preview audio..." successPrefix="Podcast production queued" /></section><section className="podcast-live-column"><article className={`podcast-card live-production-card ${liveActive ? 'streaming' : 'idle'}`}><div className="card-heading-row"><h3>Live production</h3><span className="auto-badge">{liveStatus}</span></div><div className="stage-rail">{stages.map((stage, index) => <span key={`${stage.id}-${stage.label}`} className={stage.state}>{stage.state === 'done' ? 'OK' : stage.state === 'failed' ? '!' : index + 1}<small>{stage.label}</small></span>)}</div><div className="director-note"><b>Director</b><span>{directorNote}</span></div><section className="live-transcript-section"><div className="card-heading-row"><h4>Transcript</h4><small>{transcriptRows.length ? `${transcriptRows.length} line${transcriptRows.length === 1 ? '' : 's'}` : 'Waiting for script'}</small></div><div className="live-transcript">{transcriptRows.length ? transcriptRows.map((line, index) => <p key={`${line.timestamp}-${line.speaker}-${index}`}><time>{line.timestamp}</time><b title={line.speaker}>{speakerDisplayName(line.speaker)}</b><span>{line.text}</span></p>) : <div className="live-empty-state"><strong>No live transcript yet</strong><span>Press Generate live podcast to request an LLM script and start live preview audio.</span></div>}</div></section><div className="podcast-audio-player" aria-label="Podcast audio player"><div className="audio-player-heading"><span>{currentOutput ? currentOutput.title : selectedOutputKey === '__script__' ? 'Generating podcast script...' : selectedOutputKey === '__streaming__' || selectedOutputKey === stitchedLiveKey ? 'Waiting for first stitched live audio segment...' : 'No podcast audio yet'}</span><small>{currentOutput?.live ? `LIVE STITCHED ${livePieces.length} / ${transcriptRows.length || livePieces.length}` : currentOutput ? 'AUDIO READY' : createJobMutation.isPending ? liveStatus : 'Generate a completed podcast to enable playback'}</small></div>{createJobMutation.isPending && !currentOutput ? <p className="streaming-note">The first preview chunk can auto-start. New chunks extend the same audio timeline and will not resume after you pause.</p> : null}<audio key={currentOutput?.key || selectedOutputKey || 'podcast-empty-audio'} ref={audioRef} src={currentOutput?.dataUrl ?? ''} controls preload="auto" onLoadedMetadata={onAudioLoadedMetadata} onCanPlay={() => currentOutput && setActionMessage(`${currentOutput.live ? 'Stitched live preview' : 'Audio available'}: ${currentOutput.title}`)} onPause={() => { userPausedRef.current = true; maybeInstallPendingWhilePaused(); }} onPlay={() => { userPausedRef.current = false; }} onError={() => currentOutput && setActionMessage('The generated audio could not be loaded by the browser.')} onEnded={onAudioEnded} /><div className="audio-toolbar"><label>Playback speed<select value={playbackRate} onChange={(event) => setPlaybackRate(event.target.value)}><option>0.8x</option><option>1.0x</option><option>1.25x</option></select></label><small>{currentOutput ? `${formatClock(currentOutput.duration)} ${currentOutput.live ? 'stitched preview' : 'rendered'}` : 'No audio loaded'}</small></div></div><form className="live-command" onSubmit={(event) => { event.preventDefault(); submitLiveCommand(); }}><input value={liveCommand} onChange={(event) => setLiveCommand(event.target.value)} placeholder="Add a production note" /><button type="submit" disabled={!liveCommand.trim()}>Apply</button></form></article><article className="podcast-card podcast-output-panel live-output-panel"><h3>Podcast outputs</h3><div className="output-layout"><div className="cover-art">AI<br />EVERYDAY<br />LIFE</div><div className="output-copy"><h4>{title || 'Untitled episode'} <span>{connectedJob ? String(connectedJob.status).toUpperCase() : createJobMutation.isPending ? liveStatus : 'IDLE'}</span></h4><small>{formatOptions.find((option) => option.id === format)?.label} - {speakers.length} voices - {duration}</small><p>A deep dive for {audience.toLowerCase()} in a {tone.toLowerCase()} tone with transcript and downloadable audio assets.</p>{currentOutput ? <em>{currentOutput.title}</em> : null}</div><div className="download-grid"><button type="button" disabled={!currentOutput} onClick={() => downloadCurrentOutput('MP3')}>MP3</button><button type="button" disabled={!currentOutput} onClick={() => downloadCurrentOutput('WAV')}>WAV</button><button type="button" onClick={() => setActionMessage('Transcript export requested.')}>Transcript</button><button type="button" onClick={() => setActionMessage('Show notes export requested.')}>Show Notes</button><button type="button" onClick={() => void copyEpisodeLink()}>Copy link</button></div></div></article></section><aside className="podcast-sidebar"><article className="podcast-card recent-card collapsible-card"><h3>Recent jobs</h3><div className="recent-job-list">{recentJobs.map((job) => <p key={`${job.id}-${job.status}`}><span className="recent-job-title" title={job.name}>{job.name}</span><OmnixStatusPill>{job.status}</OmnixStatusPill><small>{job.duration}</small><button type="button" onClick={() => selectRecentJob(job.id)}>Select</button></p>)}</div></article></aside></div><p className="action-toast" role="status">{actionMessage}</p></div></WorkspacePanel>;
}
